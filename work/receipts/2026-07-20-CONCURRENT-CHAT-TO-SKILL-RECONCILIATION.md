# Concurrent Chat-to-SKILL Reconciliation Receipt

A fifth candidate prompt was published concurrently after the four-prompt extension merge: `mp.instruction.chat-to-skill`.

Reconciliation completed:

- added the prompt to `registry/prompts-2026-07-20.yaml`;
- increased the extension shard count from four to five;
- updated `registry/REGISTRY_INDEX.yaml`;
- updated `work/current.yaml` to 25 registered prompts;
- added `MANIFEST_ADDENDUM_2026-07-20.yaml` because the base manifest update was blocked by provider errors;
- expanded Linear JF-104 to evaluate the fifth candidate.

Current authoritative count is 25 through the registry index and manifest addendum. The base `MANIFEST.yaml` remains stale at 24 until controlled consolidation.
